import 'package:hive/hive.dart';

const String tasksDB = 'tasks_db';

Future<void> initHive(HiveInterface hive) async {
  // _hive.registerAdapter(AppUserDtoAdapter());

  // await _hive.openBox<UserInfoDto>(userInfoDB);
}
